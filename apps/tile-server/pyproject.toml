[project]
name = "tile-server"
version = "0.1.0"
description = "RoadPulse vector tile wrapper"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
