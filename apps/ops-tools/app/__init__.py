"""Internal ops console.

A FastAPI + HTMX surface for on-call engineers: replay flood triggers, browse
k-anon violations, peek at the feature store. Strictly internal — bound to the
VPN only via the Helm chart `networkPolicy`.
"""
